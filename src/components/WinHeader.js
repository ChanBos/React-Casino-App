// Imported react libraries.
import React from 'react';
// Created an image header for the Win component page.
function WinHeader() {
    return (
        <header className="App-header">
            <div id="container">
                <img src="./images/winheader.png" alt="Win" id="win" />
            </div>
        </header>
    );
}
// Exported header to App.js.
export default WinHeader;